import http.client
import json


if __name__ == "__main__":
  # request_body = {"uid": 0, "content": ""}
  # conn = http.client.HTTPConnection("127.0.0.1", 8080)
  # conn.request("GET", "/key", json.dumps(request_body), headers={"Content-Type": "application/json"})
  # response = conn.getresponse()
  # conn.close()
  pass
